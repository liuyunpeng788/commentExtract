#服务应用相关配置
server:
  #应用端口号
  port: 9004
  servlet:
    #应用path路径
    context-path: /

#日志格式
logging:
  config: classpath:logback-boot.xml
  level:
    com:
      fosun:
        blm:
          joycity: debug

#Spring 框架相关配置
spring:
  application:
    # 应用名称
    name: joycity-blm-serv
  http:
    encoding:
      charset: UTF-8
      enabled: true
  # 数据源配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://rm-bp107r7lv7z56156m.mysql.rds.aliyuncs.com:3306/blm_serv?useUnicode=true&characterEncoding=utf-8&useSSL=false&useTimezone=true&serverTimezone=GMT%2B8
    username: blm_serv_w
    password: 93l4zVgr1Bf62Oid
    initial-size: 10
    max-active: 10
    min-idle: 5
    max-wait: 5
    pool-prepared-statements: true
    max-pool-prepared-statement-per-connection-size: 10
    max-open-prepared-statements: 10
    validation-query: select * from dual
    #validation-query-timeout:4000
    test-on-borrow: true
    test-on-return: true
    test-while-idle: false
    query-timeout: 100000
    transaction-query-timeout: 100000
    type: com.alibaba.druid.pool.DruidDataSource

  #redis配置
  redis:
    database: 6
    host: r-bp1905e577680de4.redis.rds.aliyuncs.com
    port: 6379
    password: Stargazer7d27902e770c
    timeout: 3000ms

    # 连接池最大连接数（使用负值表示没有限制） 默认 8

    lettuce:
      pool:
        max-active: 8
        # 连接池最大阻塞等待时间（使用负值表示没有限制） 默认 -1
        max-wait: 20000
        # 连接池中的最大空闲连接 默认 8
        max-idle: 8
        # 连接池中的最小空闲连接 默认 0
        min-idle: 1

  #JPA 相关配置
  jpa:
    #打印执行SQL
    show-sql: true



  #spring data 相关配置
  data:
    #关闭spring data redis 仓库的扫描
    redis:
      repositories:
        enabled: false



    mongodb:
      host: dds-bp1ecb7d067fdcf41.mongodb.rds.aliyuncs.com
      port: 3717
      username: blm_serv_user
      password: 1q2w3e
      database: blm_serv

mybatis:
  configuration:
    map-underscore-to-camel-case: true

pagehelper:
  helper-dialect: mysql
  reasonable: false
  support-methods-arguments: true
  params: count=coutSql

#应用自定义配置
core:
  #环境变量
  env: prod

# 百度api 接口调用配置
baidu_api:
  sleep: 300
  appid: 16832882
  key: sFvE4PQ1O4IhWp0QwD9yoHt8
  secret: IFoLvAT6zmE3dut6CA5Yb3slZDElSgyS


